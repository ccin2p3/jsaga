<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.globus</groupId>
    <artifactId>ws-rendezvous</artifactId>
    <version>4.0.0</version>
	<packaging>jar</packaging>
    <name>Stubs for Rendezvous service</name>
    <build>
		<sourceDirectory>java</sourceDirectory>
		<resources>
			<resource>
				<directory>resources</directory>
			</resource>
		</resources>
        <plugins>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
						<phase>generate-sources</phase>
                        <configuration>
							<tasks>
							 <property name="compile_classpath" refid="maven.compile.classpath"/>

								<!-- Build stubs for ws-rendezvous-common -->
						        <!-- factory stubs -->
								<ant antfile="${basedir}/config/globus_wsrf_build_stubs.xml" target="generateStubs">
									<property name="source.stubs.dir"
											  value="${basedir}/resources/wsdl/schema/rendezvous"/>
									<property name="wsdl.file"
										value="rendezvous_factory_service.wsdl"/>
									<property name="target.stubs.dir" location="${project.build.directory}/generated-sources"/>
								</ant>
								
								<!-- stubs -->
								<ant antfile="${basedir}/config/globus_wsrf_build_stubs.xml" target="generateStubs">
									<property name="source.stubs.dir"
											  value="${basedir}/resources/wsdl/schema/rendezvous"/>
									<property name="wsdl.file"
										value="rendezvous_service.wsdl"/>
									<property name="target.stubs.dir" location="${project.build.directory}/generated-sources"/>
								</ant>

								<!-- internal data structures for persistence -->
								<ant antfile="${basedir}/config/globus_wsrf_build_stubs.xml" target="generateStubs">
									<property name="source.stubs.dir"
											  value="${basedir}/resources/wsdl/schema/rendezvous"/>
									<property name="wsdl.file"
										value="rendezvous_data.wsdl"/>
									<property name="target.stubs.dir" location="${project.build.directory}/generated-sources"/>
								</ant>
							</tasks>
							<sourceRoot>${project.build.directory}/generated-sources</sourceRoot>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
                    </execution>	
				</executions>
            </plugin>
		</plugins>
    </build>
    <dependencies>
		<dependency>
            <groupId>org.globus</groupId>
            <artifactId>wsrf-core</artifactId>
            <version>4.0.0</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>
</project>
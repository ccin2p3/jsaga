<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <groupId>fr.in2p3.jsaga</groupId>
        <artifactId>jsaga</artifactId>
        <version>0.9.10-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <name>Main</name>
    <modelVersion>4.0.0</modelVersion>
    <groupId>fr.in2p3.commons.filesystem</groupId>
    <artifactId>file-system</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>
    <build>
        <!-- inherited -->
        <sourceDirectory>../src</sourceDirectory>
        <testSourceDirectory>../test/src</testSourceDirectory>
        <testResources>
            <testResource>
                <directory>../test/resources</directory>
            </testResource>
        </testResources>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>native-maven-plugin</artifactId>
                <version>1.0-alpha-5</version>
                <extensions>true</extensions>
                <!-- inherited -->
                <configuration>
                    <sources>
                        <source>
                            <directory>${project.build.sourceDirectory}</directory>
                            <includes>
                                <include>*.cpp</include>
                            </includes>
                        </source>
                    </sources>
                    <compilerProvider>generic</compilerProvider>
                    <compilerExecutable>g++</compilerExecutable>
                    <linkerExecutable>g++</linkerExecutable>
                </configuration>
                <!-- javah -->
                <executions>
                    <execution>
                        <id>javah</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>javah</goal>
                        </goals>
                        <configuration>
                            <javahOutputDirectory>${project.build.directory}/include</javahOutputDirectory>
                            <javahOutputFileName>file-system.h</javahOutputFileName>
                            <javahClassNames>
                                <className>fr.in2p3.commons.filesystem.FileSystem</className>
                            </javahClassNames>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- modules -->
    <modules>
        <module>file-system-all</module>
        <module>file-system-java</module>
    </modules>
    <profiles>
        <profile>
            <id>win32</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <modules>
                <module>file-system-win32</module>
            </modules>
        </profile>
        <profile>
            <id>linux</id>
            <activation>
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <modules>
                <module>libfile-system-linux</module>
            </modules>
        </profile>
    </profiles>
    <dependencies>
        <!-- needed for javah -->
        <dependency>
            <groupId>fr.in2p3.commons.filesystem</groupId>
            <artifactId>file-system-java</artifactId>
            <version>1.0</version>
        </dependency>
        <!-- inherited -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>3.8.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
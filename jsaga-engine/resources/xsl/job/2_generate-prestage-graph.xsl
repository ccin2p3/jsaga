<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet version="1.0"
            xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
            xmlns:jsdl="http://schemas.ggf.org/jsdl/2005/11/jsdl"
            xmlns:ext="http://www.in2p3.fr/jsdl-extension">
    <!-- ###########################################################################
         # Generate pre/post-staging graph
         ###########################################################################
    -->
    <xsl:output method="xml" indent="yes"/>

    <!-- entry point -->
    <xsl:template match="/">
        <xsl:apply-templates/>
    </xsl:template>
    <xsl:template match="jsdl:JobDefinition">
        <jsdl:JobDefinition>
            <xsl:comment> GENERATED BY 2_generate-prestage-graph.xsl </xsl:comment>
            <xsl:apply-templates select="@* | * | text() | comment()[position()>1]"/>
        </jsdl:JobDefinition>
    </xsl:template>

    <!-- specific templates here -->

    <!-- default template rules -->
    <xsl:template match="*">
        <xsl:element namespace="{namespace-uri()}" name="{name()}">
            <xsl:apply-templates select="@* | * | text() | comment()"/>
        </xsl:element>
    </xsl:template>
    <xsl:template match="@*|text()|comment()">
        <xsl:copy-of select="."/>
    </xsl:template>
    <xsl:template match="processing-instruction()"/>
</xsl:stylesheet>